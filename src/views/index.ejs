<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= title %></title>
    <style>
      :root {
        --bg1: #0f172a;
        --bg2: #123a76;
        --card: rgba(255, 255, 255, 0.06);
        --accent: #ffd166;
        --muted: rgba(255, 255, 255, 0.75);
        --glass: rgba(255, 255, 255, 0.04);
      }

      /* Hide scrollbar */
      *::-webkit-scrollbar {
        width: 0 !important;
        height: 0 !important;
      }
      * {
        scrollbar-width: none;
        -ms-overflow-style: none;
      }

      /* page */
      html,
      body {
        height: 100%;
      }
      body {
        margin: 0;
        font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto,
          "Helvetica Neue", Arial;
        background: linear-gradient(135deg, var(--bg1) 0%, var(--bg2) 100%);
        color: #fff;
        -webkit-font-smoothing: antialiased;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 32px;
        overflow: hidden;
      }

      /* container */
      .card {
        width: 100%;
        max-width: 900px;
        display: flex;
        gap: 32px;
        align-items: center;
        background: linear-gradient(
          180deg,
          rgba(255, 255, 255, 0.03),
          rgba(255, 255, 255, 0.02)
        );
        border-radius: 16px;
        box-shadow: 0 10px 30px rgba(5, 10, 25, 0.6);
        padding: 28px;
        border: 1px solid rgba(255, 255, 255, 0.04);
        backdrop-filter: blur(6px) saturate(120%);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        animation: fadeIn 0.8s ease forwards;
      }
      .card:hover {
        transform: translateY(-8px);
        box-shadow: 0 18px 50px rgba(5, 10, 25, 0.7);
      }

      @keyframes fadeIn {
        0% {
          opacity: 0;
          transform: translateY(20px);
        }
        100% {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* left: brand/illustration */
      .hero {
        flex: 0 0 220px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 18px;
      }
      .logo {
        width: 84px;
        height: 84px;
        border-radius: 16px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #1fb6ff, #4f46e5);
        box-shadow: 0 8px 20px rgba(12, 21, 46, 0.6);
      }
      .logo svg {
        width: 48px;
        height: 48px;
      }
      .tag {
        background: var(--glass);
        color: var(--muted);
        padding: 6px 10px;
        border-radius: 999px;
        font-size: 13px;
        letter-spacing: 0.3px;
      }

      /* right: content */
      .content {
        flex: 1;
        min-width: 0;
      }
      h1 {
        margin: 0 0 8px 0;
        font-size: 1.6rem;
        line-height: 1.05;
        letter-spacing: -0.02em;
      }
      p.lead {
        margin: 0 0 16px 0;
        color: var(--muted);
        font-size: 15px;
      }

      .meta {
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
        align-items: center;
      }
      .meta .badge {
        padding: 8px 12px;
        background: rgba(255, 255, 255, 0.04);
        border-radius: 10px;
        font-size: 13px;
        color: var(--muted);
        display: inline-flex;
        gap: 8px;
        align-items: center;
      }
      .meta .uptime {
        color: #a7f3d0;
        font-weight: 600;
      }

      /* buttons */
      .actions {
        margin-top: 18px;
        display: flex;
        gap: 12px;
        flex-wrap: wrap;
      }
      .btn {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        border: 0;
        padding: 10px 14px;
        border-radius: 10px;
        cursor: pointer;
        font-weight: 600;
        background: linear-gradient(90deg, #ffd166, #ef476f);
        color: #091633;
        text-decoration: none;
        transition: transform 0.2s ease;
      }
      .btn:hover {
        transform: translateY(-2px);
      }
      .btn.secondary {
        background: transparent;
        color: var(--muted);
        border: 1px solid rgba(255, 255, 255, 0.06);
      }

      .muted {
        color: var(--muted);
        font-size: 13px;
      }

      @media (max-width: 720px) {
        .card {
          flex-direction: column;
          padding: 20px;
          gap: 14px;
        }
        .hero {
          flex-direction: row;
          justify-content: space-between;
          width: 100%;
        }
        .logo {
          width: 64px;
          height: 64px;
        }
        h1 {
          font-size: 1.25rem;
        }
      }
    </style>
  </head>
  <body>
    <main class="card" role="main" aria-labelledby="title">
      <div class="hero" aria-hidden="true">
        <div class="logo" title="App logo">
          <svg
            viewBox="0 0 24 24"
            fill="none"
            xmlns="http://www.w3.org/2000/svg"
            aria-hidden="true"
          >
            <rect
              x="2"
              y="2"
              width="20"
              height="20"
              rx="6"
              fill="white"
              opacity="0.06"
            ></rect>
            <path
              d="M6 14c2-3 5-6 10-6"
              stroke="white"
              stroke-width="1.4"
              stroke-linecap="round"
              stroke-linejoin="round"
              opacity="0.98"
            />
            <circle
              cx="8.5"
              cy="9.5"
              r="1.6"
              fill="white"
              opacity="0.98"
            ></circle>
          </svg>
        </div>

        <div
          style="
            display: flex;
            flex-direction: column;
            gap: 8px;
            align-items: center;
          "
        >
          <span class="tag">Express • Prisma • PostgreSQL</span>
          <span class="muted" style="font-size: 12px">Server preview</span>
        </div>
      </div>

      <div class="content">
        <h1 id="title"><%= title %></h1>
        <p class="lead"><%= message %></p>

        <div class="meta" aria-live="polite">
          <div class="badge">
            Status: <strong class="uptime">Running</strong>
          </div>
          <div class="badge">
            Environment:
            <span class="muted"
              ><%= process.env.NODE_ENV || "development" %></span
            >
          </div>
          <div class="badge">
            Time:
            <span class="muted" id="realtime-clock"
              ><%= new Date().toLocaleString() %></span
            >
          </div>
        </div>

        <div class="actions">
          <a class="btn" href="/health" role="button">Check Health</a>
          <!-- <a
            class="btn secondary"
            href="https://your-frontend.example"
            target="_blank"
            rel="noopener"
            >Open Frontend</a
          > -->
        </div>

        <!-- <p class="muted" style="margin-top: 16px">
          Tip: Add useful server links here (admin, docs, dashboard).
        </p> -->
      </div>
    </main>

    <script>
      const timeSpan = document.getElementById("realtime-clock");

      function updateTime() {
        const now = new Date();
        timeSpan.textContent = now.toLocaleString();
      }

      updateTime();
      setInterval(updateTime, 1000);
    </script>
  </body>
</html>
